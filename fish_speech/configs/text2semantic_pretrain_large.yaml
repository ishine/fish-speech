defaults:
  - text2semantic_pretrain_small
  - _self_

project: text2semantic_pretrain_large_4_in_8_codebooks

# Model Configuration
model:
  ar_model:
    config:
      n_layer: 36
      n_head: 20
      dim: 1280
  nar_model:
    config:
      n_layer: 36
      n_head: 20
      dim: 1280
